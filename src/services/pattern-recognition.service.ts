/**
 * Compatibility wrapper — pattern recognition behavior now lives in PatternsService.
 */
import { PatternsService } from './patterns.service';

export class PatternRecognitionService extends PatternsService {}
